# アーカイブ

バックエンドをAWS → Firebaseに移行したため、AWS実装をアーカイブしています。

- `terraform_aws/` : Terraform (Lambda / API Gateway / DynamoDB)
- `lambda_aws/`    : Python Lambda ハンドラー
- `layers_aws/`    : Lambda Layer (qrcode)
