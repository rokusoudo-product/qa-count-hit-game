rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 認証済みユーザー（匿名認証含む）はすべてのドキュメントを読み書き可能
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
